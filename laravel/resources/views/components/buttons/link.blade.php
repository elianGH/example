<a href="{{ $route }}" class="mdc-button {{ isset($type) ? $type: '' }}">
    <span class="material-icons mdc-button__icon">{{ $icon }}</span>
    <span class="mdc-button__label">{{ $text }}</span>
</a>
